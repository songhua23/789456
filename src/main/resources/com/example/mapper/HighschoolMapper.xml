<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.HighschoolMapper">

    <select id="GetAllHighschool" resultType="highschool">
        SELECT `userName`,`head` FROM `high_user` WHERE `classname`=#{classname};
    </select>
</mapper>